# PostgreSQL
DB_PASSWORD=labeling

# Frontend (build-time, must match API_PORT mapping in docker-compose.yml)
VITE_API_URL=http://localhost:15000

# Backend build (CPU by default, override in .env.gpu for GPU mode)
# BASE_IMAGE=ubuntu:noble
# TORCH_EXTRA=cpu
# BUILD_TARGET=production

# vLLM settings (used with --profile gpu)
# VLLM_MODEL=prithivMLmods/chandra-FP8-Latest
# VLLM_GPU_UTIL=0.9
# VLLM_MAX_SEQS=4
# VLLM_MAX_MODEL_LEN=32768
# VLLM_MAX_BATCHED_TOKENS=65536
# HF_CACHE_DIR=~/.cache/huggingface
