apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vllm-hf-cache
  namespace: saegim
  labels:
    app.kubernetes.io/name: vllm
    app.kubernetes.io/part-of: saegim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
