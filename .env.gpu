# GPU build settings for docker compose
# Usage: docker compose --env-file .env --env-file .env.gpu --profile gpu up -d --build
# Or:    make up-gpu

BASE_IMAGE=nvidia/cuda:13.0.2-cudnn-runtime-ubuntu24.04
TORCH_EXTRA=cu130
